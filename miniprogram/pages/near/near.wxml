<!--miniprogram/pages/near/near.wxml-->
<view class="near">
  <search/>
  <map id="map"
    longitude="{{ longitude }}" latitude="{{ latitude }}"
    scale="14"
    show-location="true"
    markers="{{markers}}"
    bindmarkertap="markertap"></map>
</view>
